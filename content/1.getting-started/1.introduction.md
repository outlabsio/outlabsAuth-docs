---
title: Introduction
description: What OutlabsAuth is, and what it is not.
navigation:
  icon: i-lucide-house
links:
  - label: Get started
    to: /getting-started/getting-started
    icon: i-lucide-rocket
    color: primary
  - label: GitHub
    to: https://github.com/outlabsio/outlabsAuth
    icon: i-simple-icons-github
    target: _blank
    variant: subtle
    color: neutral
---

OutlabsAuth is a **library**, not a hosted login service.

You install it into your FastAPI app, point it at **your** PostgreSQL database,
mount the routers you need, and keep authentication and authorization inside
your product — without standing up a separate IdP you do not control.

## What you get

::card-group
  :::card
  ---
  icon: i-lucide-git-branch
  title: Two presets
  ---
  `SimpleRBAC` for flat roles, `EnterpriseRBAC` when you need an org tree.
  :::

  :::card
  ---
  icon: i-lucide-key-round
  title: Sign-in surface
  ---
  JWTs, API keys, invitations, optional OAuth, magic links, and phone access codes.
  :::

  :::card
  ---
  icon: i-lucide-layout-dashboard
  title: Optional sidecar UI
  to: /integrations/outlabsauth-ui
  ---
  Operator console we maintain: full management surface against any mounted host.
  :::

  :::card
  ---
  icon: i-lucide-shield
  title: Permissions that scale
  ---
  RBAC first, optional ABAC, entity hierarchy, and tree permissions.
  :::
::

## What it is not

::note
OutlabsAuth is **not** a multi-tenant SaaS IdP you sign up for, and **not** a
black-box auth microservice you must deploy separately (unless you choose to
wrap the library that way). Your product still owns customer-facing screens.
::

## Mental model

```text [your-app]
Your FastAPI app
├── OutlabsAuth (library)
│   ├── Postgres tables (users, roles, …) in your DB / schema
│   ├── Router factories you choose to mount (/auth, /users, …)
│   └── deps you use on host routes (authenticated, require_permission, …)
└── Optional: OutlabsAuth UI → talks to your mounted API
```

You decide the URL prefix (`/auth`, `/v1`, `/iam`, …). The admin UI’s
`authApiPrefix` must match that choice.

## Two presets

| If you need… | Use |
|--------------|-----|
| Users with global roles (blog, SaaS without org tree) | [SimpleRBAC](/getting-started/choosing-a-preset) |
| Hierarchy, “permissions in this team / office”, tree access | [EnterpriseRBAC](/getting-started/choosing-a-preset) |

::tip
Short chooser: [Choosing a Preset](/getting-started/choosing-a-preset). Concepts:
[Core Authorization Concepts](/enterprise/core-authorization-concepts).
::

## Next step

Continue with [Getting Started](/getting-started/getting-started) — install,
migrate, mount, and log in.
